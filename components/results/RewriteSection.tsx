// (C) Generated by Claude
import { CopyButton } from './CopyButton'
import type { RewriteData } from '@/lib/api'

interface RewriteSectionProps {
  rewrite: RewriteData
}

function SectionCard({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-6">
      <h3 className="text-sm font-semibold text-slate-500 uppercase tracking-wide mb-4">{title}</h3>
      {children}
    </div>
  )
}

function OptionCard({ label, text }: { label: string; text: string }) {
  return (
    <div className="border border-slate-100 rounded-lg p-4 bg-slate-50">
      <div className="flex items-start justify-between gap-3 mb-2">
        <span className="text-xs font-medium text-slate-400">{label}</span>
        <CopyButton text={text} />
      </div>
      <p className="text-sm text-slate-800 whitespace-pre-wrap">{text}</p>
    </div>
  )
}

export function RewriteSection({ rewrite }: RewriteSectionProps) {
  return (
    <div className="space-y-6">
      {/* Headlines */}
      <SectionCard title="Headlines">
        <div className="space-y-3">
          {rewrite.headlines.map((h, i) => (
            <OptionCard key={i} label={`Option ${i + 1}`} text={h} />
          ))}
        </div>
      </SectionCard>

      {/* About */}
      <SectionCard title="About Section">
        <div className="space-y-3">
          {rewrite.about.map((a, i) => (
            <OptionCard key={i} label={`Option ${i + 1}`} text={a} />
          ))}
        </div>
      </SectionCard>

      {/* Experience */}
      <SectionCard title="Experience">
        <div className="space-y-6">
          {rewrite.experience.map((role, i) => (
            <div key={i}>
              <div className="flex items-start justify-between gap-3 mb-2">
                <div>
                  <p className="text-sm font-medium text-slate-900">{role.title}</p>
                  <p className="text-xs text-slate-500">{role.company}</p>
                </div>
                <CopyButton text={role.bullets.join('\n')} />
              </div>
              <ul className="space-y-1.5 mt-2">
                {role.bullets.map((bullet, j) => (
                  <li key={j} className="flex gap-2 text-sm text-slate-700">
                    <span className="mt-1.5 h-1.5 w-1.5 rounded-full bg-primary shrink-0" />
                    {bullet}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </SectionCard>

      {/* Skills */}
      <SectionCard title="Skills">
        <div className="flex items-start justify-between gap-3 mb-3">
          <p className="text-xs text-slate-500">{rewrite.skills.length} skills recommended</p>
          <CopyButton text={rewrite.skills.join(', ')} />
        </div>
        <div className="flex flex-wrap gap-2">
          {rewrite.skills.map((skill, i) => (
            <span key={i} className="px-2.5 py-1 bg-primary-50 text-primary text-xs font-medium rounded-full">
              {skill}
            </span>
          ))}
        </div>
      </SectionCard>

      {/* Banner */}
      {rewrite.bannerSuggestion && (
        <SectionCard title="Banner Text Concept">
          <OptionCard label="Suggestion" text={rewrite.bannerSuggestion} />
        </SectionCard>
      )}

      {/* Tips */}
      {rewrite.tips.length > 0 && (
        <SectionCard title="Quick Tips">
          <ul className="space-y-2">
            {rewrite.tips.map((tip, i) => (
              <li key={i} className="flex gap-3 text-sm text-slate-700">
                <span className="mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary-50 text-primary text-xs font-bold">
                  {i + 1}
                </span>
                {tip}
              </li>
            ))}
          </ul>
        </SectionCard>
      )}
    </div>
  )
}
